# Docs

To create this list I first downloaded the full emoji list: https://unicode.org/emoji/charts/full-emoji-list.html

Then I scrubbed the images with the following awk commands:

awk '!/andr/' full-emoji-list.html > output.html
awk '!/cchars/' output.html > output2.html
awk "!/td class='chars'/" output2.html > output3.html


Then I used the python parse.py script to remove the links